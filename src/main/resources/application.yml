spring:
  application:
    name: Arquitetura Spring
  jpa:
    show-sql: true
  main:
    lazy-initialization: false

server:
  port: 9090

app:
  config:
    variavel: Pode por oque voce quiser aqui pois estou utilizando o value pra pegar
  montadora:
    motor-padrao: 150